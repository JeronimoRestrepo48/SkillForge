# SkillForge - Variables de Entorno
# Copiar a .env y ajustar valores

# Seguridad (generar con: python -c "import secrets; print(secrets.token_hex(50))")
SECRET_KEY=your-secret-key-here-change-in-production

# Debug (False en producción)
DEBUG=True

# Hosts permitidos (separados por coma)
ALLOWED_HOSTS=localhost,127.0.0.1

# Base de datos (SQLite por defecto en desarrollo)
# Para PostgreSQL: postgres://user:password@localhost:5432/skillforge
DATABASE_URL=sqlite:///db.sqlite3
